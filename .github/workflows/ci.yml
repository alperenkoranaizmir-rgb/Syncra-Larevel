name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup PHP
        uses: shivammathur/setup-php@v4
        with:
          php-version: '8.3'
          extensions: mbstring, bcmath, xml, sqlite
          tools: composer:v2

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Get composer cache directory
        id: composer-cache
        run: echo "COMPOSER_CACHE_DIR=$(composer config cache-files-dir)" >> $GITHUB_ENV

      - name: Cache composer files
        uses: actions/cache@v4
        with:
          path: ${{ env.COMPOSER_CACHE_DIR }}
          key: ${{ runner.os }}-composer-${{ hashFiles('**/composer.lock') }}
          restore-keys: |
            ${{ runner.os }}-composer-

      - name: Install PHP dependencies
        run: composer install --no-interaction --prefer-dist --optimize-autoloader --no-progress

      - name: Prepare .env (sqlite)
        run: |
          cp .env.example .env
          php -r "file_exists('database/database.sqlite') || mkdir('database', 0755, true); touch('database/database.sqlite');"
          php artisan key:generate --ansi
          php artisan migrate --force --ansi

      - name: Seed permissions (if package installed)
        run: php artisan db:seed --class=\Database\Seeders\PermissionSeeder --no-interaction || true

      - name: Install Node dependencies
        run: npm ci

      - name: Cache npm cache
        uses: actions/cache@v4
        with:
          path: ~/.npm
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-

      - name: Build frontend assets
        run: npm run build

      - name: Run basic tests
        run: php artisan test --no-interaction || true

  # Optional deploy job skeleton. Uncomment and configure if you want automated deployment
  #deploy:
  #  needs: build
  #  runs-on: ubuntu-latest
  #  if: github.ref == 'refs/heads/main'
  #  steps:
  #    - uses: actions/checkout@v4
  #    - name: Deploy (example)
  #      run: |
  #        echo "Add deployment commands here (rsync, scp, or your cloud provider CLI)."
  #      env:
  #        SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
  #        DEPLOY_HOST: ${{ secrets.DEPLOY_HOST }}
